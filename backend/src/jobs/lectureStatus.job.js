import { sendLectureReminderEmail } from '../services/emailService.js';

// ─── CRON JOB: CHECK LECTURES STARTING SOON ───────────────
export const checkUpcomingLectures = async () => {
    // try {
    //   await sendLectureReminderEmail(to, name, title, time, link);
    // } catch (err) { ... }
};
