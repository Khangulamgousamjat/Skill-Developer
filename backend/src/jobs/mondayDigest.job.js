import { sendManagerWeeklyDigest } from '../services/emailService.js';

// ─── CRON JOB: MONDAY MORNING MANAGER DIGEST ───────────────
export const checkMondayDigest = async () => {
    // try {
    //   await sendManagerWeeklyDigest(manager.email, manager.full_name, stats);
    // } catch (err) { ... }
};
