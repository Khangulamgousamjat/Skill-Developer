{
  "name": "ssllm-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run dev --prefix app_frontend",
    "build": "npm run build --prefix app_frontend",
    "install": "npm install --prefix app_frontend",
    "postinstall": "npm install --prefix backend"
  }
}
